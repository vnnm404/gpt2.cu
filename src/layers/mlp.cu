/* MLP layer implementation - Template implementations are in the header */

#include "gpt2/layers/mlp.h"

// All MLP kernel implementations are now templates in mlp.h
// This file is kept for any non-template helper functions if needed in the future
